---
name: react-with-gesture
---

import { useSpring, animated, config } from "react-spring";
import { Playground } from "docz";

# react-with-gesture

This hook allows you to get params from a drag operation, so that you can alter the props of an element (or use it together with react-spring).
